// ─── MOCK_MODE flag ───────────────────────────────────────────────────────────
// Set to true to bypass real API calls. Set to false to use real backend.
export const MOCK_MODE = false;

export const API_BASE_URL = 'https://ecoride-tx0l.onrender.com/api';
export const SOCKET_URL = 'https://ecoride-tx0l.onrender.com';

export const STORAGE_KEYS = {
  token: 'ecoride_token',
  user: 'ecoride_user',
};
